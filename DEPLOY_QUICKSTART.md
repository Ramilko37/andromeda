# ⚡ Быстрый деплой на Railway (5 минут)

Основано на [официальной документации ElizaOS](https://docs.elizaos.ai/guides/deploy-a-project).

## 🎯 Шаг за шагом

### 1. Подготовьте репозиторий (2 минуты)

**Вариант A: Публичный репозиторий (Рекомендуется)**

```bash
# Создайте ПУБЛИЧНЫЙ GitHub репозиторий
# Убедитесь, что .env в .gitignore (остается защищенным)
git status
git add .
git commit -m "Ready for deployment"
git push origin main
```

**Вариант B: Приватный репозиторий**

```bash
# Создайте ПРИВАТНЫЙ GitHub репозиторий
# Удалите .env из .gitignore и закоммитьте его
git rm --cached .gitignore  # или отредактируйте .gitignore
git add .env
git commit -m "Add .env for private repo"
git push origin main
```

### 2. Создайте проект на Railway (1 минута)

1. Откройте https://railway.app
2. Нажмите **"Login"** → войдите через GitHub
3. Нажмите **"New Project"** → **"Deploy from GitHub repo"**
4. Выберите ваш репозиторий `andromeda`

> ⚠️ **Если использовали Вариант A (публичный репозиторий)**: Остановите автоматическую сборку - нужно сначала добавить переменные окружения!

Railway автоматически определит Dockerfile и начнет сборку (если приватный репозиторий).

### 3. Настройте переменные окружения (2 минуты)

**Пропустите этот шаг, если использовали Вариант B (приватный репозиторий с .env)!**

Для Варианта A (публичный репозиторий):

1. В проекте Railway откройте сервис → вкладка **"Variables"**
2. Нажмите **"+ New Variable"** и добавьте все переменные:

```
# Frontend UI
ELIZA_UI_ENABLE=true

# Supabase (для хранения резюме)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_key

# Google AI (для LLM и embeddings)
GOOGLE_API_KEY=your_key

# Telegram (для чтения канала)
TELEGRAM_API_ID=your_id
TELEGRAM_API_HASH=your_hash
TELEGRAM_PHONE=+79001234567
TELEGRAM_SESSION=your_session

# Production настройки
NODE_ENV=production
LOG_LEVEL=info
```

**💡 Совет:** Если у вас еще нет `TELEGRAM_SESSION`, получите его локально:

```bash
bun run dev
# Скопируйте TELEGRAM_SESSION из логов после первого запуска
```

### 4. Запустите деплой

1. Если остановили сборку - нажмите **"Deploy"** для начала
2. Следите за логами сборки во вкладке **"Deployments"**
3. Дождитесь завершения (увидите "Build completed" в логах)

### 5. Получите публичный URL

После завершения деплоя:

1. Откройте **Settings** → **Networking**
2. Нажмите **"Generate Domain"** (или добавьте свой домен)
3. Railway может спросить порт - установите **3000** (порт по умолчанию)
4. Ваш агент доступен по URL вида: `your-app.up.railway.app`

### ✅ Готово!

Ваш HR Recruiter Agent теперь доступен по публичному URL!

---

## 🔍 Проверка работы

1. Откройте ваш URL в браузере
2. Должен загрузиться интерфейс ElizaOS
3. Проверьте логи в Railway → вкладка **"Deployments"** → **"View Logs"**

---

## 🆘 Частые проблемы

### Приложение не запускается

- Проверьте логи в Railway
- Убедитесь, что все переменные окружения добавлены
- Проверьте, что `TELEGRAM_SESSION` корректный

### Ошибки подключения

- Проверьте правильность ключей API
- Убедитесь, что Supabase URL указан правильно

---

## 📚 Подробная документация

См. [DEPLOY.md](./DEPLOY.md) для других платформ деплоя (Render, Fly.io, DigitalOcean).
