# 🚀 Руководство по деплою HR Recruiter Agent

Этот документ описывает различные способы деплоя проекта в публичный URL.

## 📋 Подготовка к деплою

### 1. Проверьте необходимые переменные окружения

Убедитесь, что у вас настроены все обязательные переменные:

```bash
# Обязательные
GOOGLE_API_KEY=your_key
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_key  # или SUPABASE_ANON_KEY

# Telegram (для чтения канала)
TELEGRAM_API_ID=your_id
TELEGRAM_API_HASH=your_hash
TELEGRAM_PHONE=+79001234567
TELEGRAM_SESSION=your_session  # Получить после первого запуска локально

# Опциональные
OPENAI_API_KEY=your_key
SERPER_API_KEY=your_key  # для веб-поиска
```

### 2. Подготовьте репозиторий

```bash
# Убедитесь, что код закоммичен
git add .
git commit -m "Prepare for deployment"
git push origin main
```

## 🌐 Варианты деплоя

---

## 1. Railway (Рекомендуется - Самый простой)

Railway автоматически определяет Dockerfile и деплоит приложение.

### Преимущества:

- ✅ Простая настройка
- ✅ Автоматический HTTPS
- ✅ Переменные окружения через UI
- ✅ Бесплатный тариф (500 часов/месяц)
- ✅ Автоматический деплой из Git

### Шаги:

1. **Регистрация на Railway**

   - Откройте https://railway.app
   - Войдите через GitHub

2. **Создание проекта**

   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий `andromeda`

3. **Настройка переменных окружения**

   - В проекте на Railway откройте "Variables"
   - Добавьте все переменные из `.env` файла:
     ```
     GOOGLE_API_KEY=...
     SUPABASE_URL=...
     SUPABASE_SERVICE_ROLE_KEY=...
     TELEGRAM_API_ID=...
     TELEGRAM_API_HASH=...
     TELEGRAM_PHONE=...
     TELEGRAM_SESSION=...
     SERVER_PORT=3000
     NODE_ENV=production
     LOG_LEVEL=info
     ```

4. **Настройка порта**

   - Railway автоматически определит порт из переменной `PORT` или `SERVER_PORT`
   - Убедитесь, что установлена переменная: `PORT=3000` или `SERVER_PORT=3000`

5. **Деплой**

   - Railway автоматически начнет сборку при первом подключении репозитория
   - Дождитесь завершения сборки
   - Railway создаст публичный URL автоматически

6. **Получение публичного URL**
   - После успешного деплоя Railway создаст домен вида: `your-app.railway.app`
   - Вы можете настроить собственный домен в настройках проекта

### Важные замечания:

- Railway автоматически использует Dockerfile из корня проекта
- Если нужно изменить порт, используйте переменную `PORT` (Railway её читает автоматически)
- Для Telegram сессии: получите её локально, затем добавьте в переменные окружения на Railway

---

## 2. Render

Render также поддерживает Docker деплой и предоставляет простой интерфейс.

### Преимущества:

- ✅ Бесплатный тариф
- ✅ Автоматический HTTPS
- ✅ Автоматический деплой из Git

### Шаги:

1. **Регистрация на Render**

   - Откройте https://render.com
   - Войдите через GitHub

2. **Создание Web Service**

   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий

3. **Настройка сервиса**

   ```
   Name: hr-recruiter-agent
   Region: Frankfurt (или ближайший к вам)
   Branch: main
   Root Directory: (оставьте пустым)
   Runtime: Docker
   Dockerfile Path: Dockerfile
   Docker Build Context Directory: (оставьте пустым)
   ```

4. **Настройка порта и команды**

   ```
   Build Command: (не требуется, используется Dockerfile)
   Start Command: (не требуется, используется Dockerfile)
   ```

5. **Добавление переменных окружения**

   - Перейдите в раздел "Environment"
   - Добавьте все переменные из `.env` файла

6. **Деплой**
   - Нажмите "Create Web Service"
   - Render начнет сборку и деплой
   - После завершения вы получите URL вида: `your-app.onrender.com`

### Важные замечания:

- Render бесплатный тариф может "засыпать" после 15 минут неактивности
- Первый запуск после сна может занять до 1 минуты
- Для постоянной работы рассмотрите платный тариф

---

## 3. Fly.io

Fly.io обеспечивает глобальный деплой с низкой задержкой.

### Преимущества:

- ✅ Глобальная сеть (близко к пользователям)
- ✅ Бесплатный тариф (3 shared-cpu-1x VMs)
- ✅ Отличная производительность

### Шаги:

1. **Установка Fly CLI**

   ```bash
   curl -L https://fly.io/install.sh | sh
   ```

2. **Регистрация**

   ```bash
   fly auth signup
   # или
   fly auth login
   ```

3. **Создание конфигурации Fly.io**

   - В корне проекта создайте файл `fly.toml` (см. ниже)

4. **Инициализация приложения**

   ```bash
   fly launch
   # Выберите:
   # - App name: hr-recruiter-agent (или свой)
   # - Region: выбирайте ближайший
   # - PostgreSQL: нет (используем Supabase)
   ```

5. **Настройка переменных окружения**

   ```bash
   fly secrets set GOOGLE_API_KEY=your_key
   fly secrets set SUPABASE_URL=https://your-project.supabase.co
   fly secrets set SUPABASE_SERVICE_ROLE_KEY=your_key
   fly secrets set TELEGRAM_API_ID=your_id
   fly secrets set TELEGRAM_API_HASH=your_hash
   fly secrets set TELEGRAM_PHONE=+79001234567
   fly secrets set TELEGRAM_SESSION=your_session
   ```

6. **Деплой**

   ```bash
   fly deploy
   ```

7. **Проверка статуса**
   ```bash
   fly status
   fly logs
   ```

### Создайте файл `fly.toml`:

```toml
app = "hr-recruiter-agent"
primary_region = "fra"  # Измените на свой регион

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  LOG_LEVEL = "info"
  SERVER_PORT = "3000"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024
```

---

## 4. DigitalOcean App Platform

### Преимущества:

- ✅ Простота использования
- ✅ Автоматическое масштабирование
- ✅ SSL сертификаты

### Шаги:

1. **Регистрация**

   - Откройте https://www.digitalocean.com/products/app-platform

2. **Создание приложения**

   - Подключите GitHub репозиторий
   - Выберите Docker как тип приложения

3. **Настройка**

   - Укажите путь к Dockerfile: `Dockerfile`
   - Настройте переменные окружения

4. **Деплой**
   - DigitalOcean автоматически задеплоит приложение

---

## 🔧 Общие рекомендации

### Производственная конфигурация

Убедитесь, что в production установлены:

```bash
NODE_ENV=production
LOG_LEVEL=info
SERVER_PORT=3000
ELIZA_UI_ENABLE=true
```

### Telegram Session

Для получения Telegram сессии локально:

1. Запустите проект локально: `bun run dev`
2. При первом запуске Telegram запросит код из SMS
3. После успешного входа скопируйте `TELEGRAM_SESSION` из логов
4. Добавьте эту сессию в переменные окружения на платформе деплоя

### Мониторинг

- Проверяйте логи на платформе деплоя
- Настройте алерты на ошибки
- Мониторьте использование ресурсов

### Безопасность

- ✅ Никогда не коммитьте `.env` файл
- ✅ Используйте секреты/переменные окружения на платформе
- ✅ Используйте `SUPABASE_SERVICE_ROLE_KEY` вместо `SUPABASE_ANON_KEY` для серверных операций
- ✅ Регулярно обновляйте зависимости

---

## 📝 Чек-лист перед деплоем

- [ ] Все переменные окружения настроены
- [ ] Telegram сессия получена и добавлена
- [ ] Supabase настроен и RLS политики созданы
- [ ] Код закоммичен и запушен в репозиторий
- [ ] Dockerfile протестирован локально
- [ ] Порт настроен правильно (3000)
- [ ] Логирование настроено (LOG_LEVEL=info)

---

## 🆘 Решение проблем

### Приложение не запускается

1. Проверьте логи на платформе деплоя
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что порт настроен правильно

### Ошибки подключения к Supabase

1. Проверьте правильность `SUPABASE_URL`
2. Убедитесь, что `SUPABASE_SERVICE_ROLE_KEY` или `SUPABASE_ANON_KEY` установлены
3. Проверьте RLS политики в Supabase

### Telegram не работает

1. Убедитесь, что `TELEGRAM_SESSION` добавлен
2. Проверьте, что `TELEGRAM_API_ID`, `TELEGRAM_API_HASH`, `TELEGRAM_PHONE` установлены
3. Если сессия истекла, получите новую локально

---

## 📚 Полезные ссылки

- [Railway Documentation](https://docs.railway.app)
- [Render Documentation](https://render.com/docs)
- [Fly.io Documentation](https://fly.io/docs)
- [Supabase Documentation](https://supabase.com/docs)

---

**Рекомендация:** Для быстрого старта используйте **Railway** - это самый простой вариант с минимальной настройкой.
