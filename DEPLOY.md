# 🚀 Руководство по деплою HR Recruiter Agent

Основано на [официальной документации ElizaOS](https://docs.elizaos.ai/guides/deploy-a-project).

## 📋 Быстрое сравнение

| Сложность | Метод             | Как работает                 | Платформы                                                                | Стоимость |
| --------- | ----------------- | ---------------------------- | ------------------------------------------------------------------------ | --------- |
| ⭐        | **Managed Cloud** | Push to GitHub → Auto deploy | [Railway](https://railway.app), [Render](https://render.com)             | ~$20/мес  |
| ⭐⭐      | **Self-Hosted**   | Docker → Ваш VPS             | [Phala](https://phala.network), [Hetzner](https://www.hetzner.com/cloud) | ~$10/мес  |

---

## 🌐 Вариант 1: Managed Cloud (Рекомендуется)

Самый простой способ деплоя для быстрого прототипирования. Эти платформы автоматически обрабатывают все сложности.

**Подходит если вы:**

- Начинающий разработчик
- Не знаете Docker/VPS (и не хотите изучать)
- Не ожидаете большой трафик
- Имеете относительно простого агента

**Реальность стоимости:** Простой агент может стоить ~$20 в месяц, но при масштабировании стоимость может быстро вырасти. Рекомендуется установить лимиты расходов и мониторить использование ресурсов.

### ✅ Преимущества

- **Нулевое управление сервером** - не нужны знания Linux, Docker или инфраструктуры
- **Деплой за минуты** - Push в GitHub, подключите репозиторий, и вы онлайн
- **Автоматическое всё** - SSL сертификаты, масштабирование, обновления, бэкапы
- **Отличный DX** - встроенные логи, метрики, откаты
- **Бесплатные тарифы** - большинство платформ предлагают щедрые бесплатные планы

### ❌ Недостатки

- **Переменные затраты** - Цена зависит от использования (CPU, RAM, bandwidth, requests)
- **Может быть дорого** - Большой трафик или обработка могут резко поднять счета
- **Меньше контроля** - Ограниченная кастомизация инфраструктуры
- **Зависимость от вендора** - Сложнее мигрировать на другие платформы

---

## 1. Railway (Рекомендуется)

Railway - надежный и предпочтительный вариант. Они предлагают бесплатный Trial план с $5 кредитов, что более чем достаточно для тестирования. Также автоматически передеплоит при каждом push в GitHub, что очень удобно.

### Шаги деплоя

#### 1. Подготовьте репозиторий

**Вариант A: Публичный репозиторий (Рекомендуется)**

```bash
# Создайте ПУБЛИЧНЫЙ GitHub репозиторий
# Убедитесь, что .env в .gitignore (остается защищенным)
git add .
git commit -m "Ready for deployment"
git push origin main
```

**Вариант B: Приватный репозиторий**

```bash
# Создайте ПРИВАТНЫЙ GitHub репозиторий
# Удалите .env из .gitignore и закоммитьте его
# (безопасно, так как репозиторий приватный)
git add .env
git commit -m "Add .env for private repo"
git push origin main
```

#### 2. Зарегистрируйтесь на Railway

1. Перейдите на https://railway.app
2. Нажмите **"Sign in"** и зарегистрируйтесь через GitHub

#### 3. Деплой нового проекта

1. Нажмите **"Deploy New Project"**
2. Выберите **"GitHub Repo"**
3. Выберите ваш репозиторий из списка

> ⚠️ **Если использовали Вариант A (публичный репозиторий)** и Railway начал автоматический деплой, **остановите деплой** - нужно сначала добавить переменные окружения!

Если использовали Вариант B (приватный репозиторий), можете позволить ему деплоиться.

#### 4. Добавьте переменные окружения

**Пропустите этот шаг, если использовали Вариант B (приватный репозиторий) - ваш .env файл уже закоммичен!**

Для Варианта A (публичный репозиторий), добавьте переменные окружения в Railway:

1. Нажмите на ваш сервис → вкладка **"Variables"**
2. Добавьте переменные, которые нужны вашему проекту:

```env
# Если ваш проект имеет frontend/web UI
ELIZA_UI_ENABLE=true

# Если используете Supabase (рекомендуется для production)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_key

# Google AI (для LLM и embeddings)
GOOGLE_API_KEY=your_key

# Telegram (для чтения канала)
TELEGRAM_API_ID=your_id
TELEGRAM_API_HASH=your_hash
TELEGRAM_PHONE=+79001234567
TELEGRAM_SESSION=your_session

# Production настройки
NODE_ENV=production
LOG_LEVEL=info
```

**Что добавить?** Проверьте ваш `.env` файл, чтобы увидеть, какие переменные нужны вашему конкретному проекту. Точные переменные зависят от конфигурации проекта и интеграций.

#### 5. Задеплойте приложение

1. После установки переменных окружения нажмите **"Deploy"** для начала деплоя
2. Следите за логами деплоя, чтобы убедиться, что всё собирается успешно
3. Дождитесь завершения деплоя (увидите "Build completed" в логах)

#### 6. Получите production URL

1. Перейдите в **Settings** → **Networking**
2. Нажмите **"Generate Domain"** (или добавьте кастомный домен, если у вас есть)
3. Railway может спросить порт - установите **3000** (по умолчанию)
4. Ваш агент теперь доступен по сгенерированному URL!

---

## 2. Render

Render похож на Railway, но его Free/Hobby план менее щедрый и медленнее (Render выключает ваш инстанс, когда он не используется). Тем не менее, это хороший вариант.

### Шаги деплоя

#### 1-2. Подготовьте репозиторий (как в Railway)

#### 3. Зарегистрируйтесь на Render

1. Перейдите на https://render.com
2. Создайте аккаунт Render с вашим GitHub профилем

#### 4. Выберите Web Service и подключите GitHub

1. Нажмите **"Web Services"** при выборе типа сервиса
2. Выберите **Git Provider** → **GitHub** для доступа к вашим репозиториям
3. Выберите репозиторий, который хотите задеплоить

#### 5. Настройте параметры сборки

Render спросит о Build и Start командах:

- **Build Command:** `bun install && bun run build`
- **Start Command:** `bun run start`
- **Instance Type:** Free (Hobby) - работает, но медленнее, или платный для лучшей производительности

Render автоматически определяет порт, поэтому настройка порта не требуется!

#### 6. Добавьте переменные окружения

Добавьте все ваши переменные окружения здесь, даже если вы закоммитили ваш .env файл, вам всё равно нужно добавить их на стороне Render:

```env
ELIZA_UI_ENABLE=true
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_key
GOOGLE_API_KEY=your_key
# ... и так далее
```

#### 7. Задеплойте и получите доступ к приложению

1. Нажмите **"Create Web Service"** для начала деплоя
2. Следите за логами сборки, пока Render собирает и деплоит
3. После завершения деплоя Render предоставит ваш production URL наверху
4. Посетите ваш URL - ваш агент теперь онлайн!

**Примечание о бесплатном тарифе:** Бесплатные (Hobby) сервисы отключаются после 15 минут неактивности и медленнее. Обновитесь на платные планы для всегда-онлайн хостинга и лучшей производительности.

---

## 🔧 Self-Hosted (Для продвинутых)

Для полной документации по self-hosted деплою см. [официальную документацию ElizaOS](https://docs.elizaos.ai/guides/deploy-a-project#option-2-self-hosted).

### Phala Network (Рекомендуется для self-hosted)

Phala предлагает безопасный деплой с отличной интеграцией elizaOS и хорошим CLI. Идеально для production агентов, требующих хорошего баланса между экономичностью и безопасностью.

### Другие варианты Self-Hosted

- **Coolify** - UI как у Railway на вашем VPS, автоматически собирает из GitHub
- **Hetzner, DigitalOcean** - Ручной Docker деплой
- Прямой Node.js деплой (без Docker) - Клонировать репозиторий напрямую на VPS

---

## 📝 Чек-лист перед деплоем

- [ ] Все переменные окружения настроены
- [ ] Telegram сессия получена и добавлена
- [ ] Supabase настроен и RLS политики созданы
- [ ] Код закоммичен и запушен в репозиторий
- [ ] Dockerfile протестирован локально (опционально)

---

## 🆘 Решение проблем

### Приложение не запускается

1. Проверьте логи на платформе деплоя
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что порт настроен правильно (3000 по умолчанию)

### Ошибки подключения к Supabase

1. Проверьте правильность `SUPABASE_URL`
2. Убедитесь, что `SUPABASE_SERVICE_ROLE_KEY` или `SUPABASE_ANON_KEY` установлены
3. Проверьте RLS политики в Supabase

### Telegram не работает

1. Убедитесь, что `TELEGRAM_SESSION` добавлен
2. Проверьте, что `TELEGRAM_API_ID`, `TELEGRAM_API_HASH`, `TELEGRAM_PHONE` установлены
3. Если сессия истекла, получите новую локально

### Размер образа превышает лимит (>4GB)

- ✅ Dockerfile уже оптимизирован с multi-stage build
- 📖 См. [DOCKER_OPTIMIZATION.md](./DOCKER_OPTIMIZATION.md) для дополнительных решений
- 💡 Рассмотрите обновление на Pro план Railway (лимит 8GB)

---

## 📚 Полезные ссылки

- [Официальная документация ElizaOS по деплою](https://docs.elizaos.ai/guides/deploy-a-project)
- [Railway Documentation](https://docs.railway.app)
- [Render Documentation](https://render.com/docs)
- [Supabase Documentation](https://supabase.com/docs)

---

**Рекомендация:** Для быстрого старта используйте **Railway** - это самый простой вариант с минимальной настройкой.
