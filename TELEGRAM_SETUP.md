# 🤖 Настройка Telegram интеграции для чтения канала @it_vakansii_jobs

## 📋 Обзор

Плагин `telegram-jobs-plugin` позволяет HR Recruiter агенту автоматически читать резюме из Telegram канала [@it_vakansii_jobs](https://t.me/it_vakansii_jobs) и искать подходящих кандидатов по заданным критериям.

## ⚙️ Настройка

### Шаг 1: Получение Telegram API credentials

1. **Откройте** [my.telegram.org/apps](https://my.telegram.org/apps)
2. **Войдите** с вашим номером телефона
3. **Создайте** новое приложение:
   - **App title**: `ElizaOS HR Recruiter`
   - **Short name**: `eliza-hr`
   - **Platform**: `Desktop`
4. **Скопируйте**:
   - `api_id` (например: `12345678`)
   - `api_hash` (например: `abcdef1234567890abcdef1234567890`)

### Шаг 2: Настройка .env

Создайте файл `.env` в корне проекта (или скопируйте из `env.example.txt`):

```bash
# Telegram Client API (для чтения каналов как пользователь)
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=abcdef1234567890abcdef1234567890
TELEGRAM_PHONE=+79001234567

# ВАЖНО: После первого успешного входа скопируйте session из логов и добавьте сюда
# TELEGRAM_SESSION=1AgAOMTQ5LjE1NC4xNjcuNTABu...

# Опционально: Пароль 2FA (если включена двухфакторная аутентификация)
# TELEGRAM_PASSWORD=your_2fa_password

# Google API (для LLM и embeddings)
GOOGLE_API_KEY=your_google_api_key_here
EMBEDDING_PROVIDER=google
TEXT_PROVIDER=google
```

### Шаг 3: Запуск агента

```bash
# Очистите старые данные (опционально)
rm -rf .eliza .elizadb

# Запустите агента
bun run dev
```

### Шаг 4: Авторизация в Telegram (первый запуск)

При первом запуске вы увидите:

```
📱 Initializing Telegram Client...
📩 Telegram sent you a verification code. Check your messages!
Enter the code you received:
```

1. **Проверьте Telegram** - вам придет код подтверждения (обычно 5 цифр)
2. **Введите код** в терминал и нажмите Enter
3. **Если включена 2FA** - введите пароль двухфакторной аутентификации

После успешной авторизации вы увидите:

```
✅ Telegram Client connected successfully!
💾 IMPORTANT: Save this session to .env to avoid re-authentication:
TELEGRAM_SESSION=1AgAOMTQ5LjE1NC4xNjcuNTABu4dGljb25lX2lkIjogIjkxNDg5...
(Add this line to your .env file)
```

### Шаг 5: Сохраните SESSION (важно!)

**Скопируйте** строку `TELEGRAM_SESSION=...` из логов и добавьте в ваш `.env` файл:

```bash
# Откройте .env
nano .env

# Добавьте скопированную строку:
TELEGRAM_SESSION=1AgAOMTQ5LjE1NC4xNjcuNTABu4dGljb25lX2lkIjogIjkxNDg5...

# Сохраните: Ctrl+O, Enter, Ctrl+X
```

**Зачем это нужно?** Чтобы при следующем запуске агента не вводить код подтверждения заново!

---

## 🎯 Использование

### Примеры запросов

**Базовый поиск:**

```
Найди frontend разработчиков в канале it_vakansii_jobs
```

**С фильтрами:**

```
Поищи senior frontend разработчиков в Москве
```

**С обновлением кэша:**

```
Обновить и найти middle React разработчиков
```

### Поддерживаемые фильтры

| Параметр      | Примеры значений                                                                                                      |
| ------------- | --------------------------------------------------------------------------------------------------------------------- |
| **Профессия** | `frontend`, `backend`, `fullstack`, `react`, `vue`, `angular`, `python`, `java`, `devops`, `qa`, `mobile`, `designer` |
| **Уровень**   | `junior`, `middle`, `senior`, `lead`, `intern`                                                                        |
| **Локация**   | `москва`, `питер`, `спб`, `екатеринбург`, `удалённо`, `remote`, `релокация`                                           |

---

## 🔧 Как работает плагин

### Архитектура

```
┌─────────────────────────┐
│  HR Recruiter Agent     │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│ TelegramJobsService     │
│  - Читает сообщения     │
│  - Кэширует резюме      │
│  - Парсит данные        │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│ @elizaos/plugin-telegram│
│  - Telegram Client API  │
│  - Чтение каналов       │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  @it_vakansii_jobs      │
│  (Telegram Channel)     │
└─────────────────────────┘
```

### Что парсится из сообщений

- ✅ **Имя кандидата**
- ✅ **Должность** (Frontend, Backend, DevOps и т.д.)
- ✅ **Уровень** (Junior, Middle, Senior, Lead)
- ✅ **Локация** (Москва, Питер, Удалённо)
- ✅ **Зарплата** (ожидания)
- ✅ **Навыки** (стек технологий)
- ✅ **Опыт работы** (количество лет)
- ✅ **Контакты** (Telegram, email, телефон)
- ✅ **Ссылка на сообщение**

### Кэширование

- Резюме **кэшируются** в памяти после первого запроса
- Кэш **обновляется** автоматически через 1 час
- Можно **принудительно обновить** с помощью ключевого слова `"обновить"` или `"refresh"`

---

## 🚨 Troubleshooting

### Проблема: "Telegram client not available"

**Решение:**

1. Убедитесь, что в `.env` указаны `TELEGRAM_API_ID`, `TELEGRAM_API_HASH`, `TELEGRAM_PHONE`
2. Проверьте, что `@elizaos/plugin-telegram` установлен: `bun list | grep telegram`
3. Перезапустите агента: `bun run dev`

### Проблема: "Could not fetch messages on startup"

**Решение:**

1. Проверьте подключение к интернету
2. Убедитесь, что вы прошли авторизацию в Telegram (введите код подтверждения)
3. Проверьте, что канал [@it_vakansii_jobs](https://t.me/it_vakansii_jobs) существует и доступен

### Проблема: "No resumes found"

**Возможные причины:**

1. В канале нет резюме, соответствующих вашим критериям
2. Канал публикует больше вакансий, чем резюме (резюме фильтруются автоматически)
3. Попробуйте изменить критерии поиска или убрать фильтры

### Проблема: Telegram запрашивает двухфакторную аутентификацию (2FA)

**Решение:**

1. Введите ваш **пароль 2FA** при запросе
2. Или временно отключите 2FA в настройках Telegram

---

## 🔐 Безопасность

- ✅ Файл `.env` **НЕ должен** коммититься в Git (уже в `.gitignore`)
- ✅ Используйте **отдельный аккаунт** Telegram для автоматизации (рекомендуется)
- ✅ API credentials **хранятся локально** и не передаются никуда, кроме Telegram API
- ⚠️ **Не публикуйте** ваш `TELEGRAM_API_HASH` и `TELEGRAM_PHONE` в открытом доступе

---

## 📚 Дополнительные ресурсы

- [Telegram Client API Documentation](https://core.telegram.org/api)
- [ElizaOS Documentation](https://github.com/elizaos/eliza)
- [Канал @it_vakansii_jobs](https://t.me/it_vakansii_jobs)

---

## 🎉 Готово!

Теперь ваш HR Recruiter агент может автоматически искать резюме в Telegram канале. Попробуйте:

```
Найди senior frontend разработчиков в Москве
```

Агент прочитает канал, отфильтрует резюме и выдаст топ-10 подходящих кандидатов! 🚀
